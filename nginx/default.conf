server {
    listen 80;

    # root /usr/share/nginx/html;

    # htmls of Potree
    location /examples/ {
        alias /usr/share/nginx/potree/examples/;
        # index index.html;
        autoindex on;
    }

    # htmls of original data 
    # location /hoge/ {
    #     alias /usr/share/nginx/hoge/;
    #     # index index.html;
    #     autoindex on;
    # }

    # fallback: 404
    location / {
        return 404;
    }
}